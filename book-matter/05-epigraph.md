# Epigraph (book-level)

*[Optional book-opening quotation, or a single line, set in italics on its own page.]*

*(Optional. Each chapter already carries its own epigraph per the book's format, so a book-level epigraph is not needed — include one only if a single line frames the whole novel for you. If used, clear permissions for any copyrighted quotation.)*
